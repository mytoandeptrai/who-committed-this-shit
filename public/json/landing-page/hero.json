{"history":[{"breakpoints":[{"min":992,"name":"Desktop","props":{"size":0.39,"density":34,"spread":0.65,"skew":0.55,"variance":0.73,"laneWidth":0.3},"max":null},{"min":0,"name":"Mobile","props":{"density":60,"size":0.46,"laneWidth":0.17,"variance":1,"skew":1,"spread":1},"max":575}],"visible":true,"aspectRatio":1,"userDownsample":1,"layerType":"effect","type":"blocks","usesPingPong":false,"texture":false,"animating":true,"mouseMomentum":0,"mouseSpring":0,"isMask":0,"compiledFragmentShaders":["#version 300 es\nprecision highp float;in vec2 vTextureCoord;uniform sampler2D uTexture;\nuniform int uDensity;\nuniform float uSize;\nuniform float uSpread;\nuniform float uLaneWidth;\nuniform float uVariance;\nuniform float uSkew;\nuniform float uTime;uniform vec2 uResolution;out vec4 fragColor;const int MAX_BLOCKS = 60;\nconst float EPSILON = 0.0001;\nconst float PI = 3.14159265359;\nconst float TAU = 6.28318530718;vec2 hash21(float n) {\nreturn fract(sin(vec2(n, n + 17.0) * vec2(127.1, 311.7)) * 43758.5453123);\n}vec3 getRayDirection(vec2 uv, float aspect, float focalLength) {\nvec2 screen = vec2((uv.x - vec2(0.5, 0.5).x) * aspect, uv.y - vec2(0.5, 0.5).y);\nreturn normalize(vec3(screen, focalLength));\n}bool intersectAABB(vec3 rayOrigin, vec3 rayDir, vec3 boxMin, vec3 boxMax) {\nvec3 safeRayDir = vec3(\nabs(rayDir.x) < EPSILON ? (rayDir.x < 0.0 ? -EPSILON : EPSILON) : rayDir.x,\nabs(rayDir.y) < EPSILON ? (rayDir.y < 0.0 ? -EPSILON : EPSILON) : rayDir.y,\nabs(rayDir.z) < EPSILON ? (rayDir.z < 0.0 ? -EPSILON : EPSILON) : rayDir.z\n);\nvec3 t0 = (boxMin - rayOrigin) / safeRayDir;\nvec3 t1 = (boxMax - rayOrigin) / safeRayDir;\nvec3 tMin3 = min(t0, t1);\nvec3 tMax3 = max(t0, t1);\nfloat tMin = max(max(tMin3.x, tMin3.y), max(tMin3.z, 0.0));\nfloat tMax = min(min(tMax3.x, tMax3.y), tMax3.z);\nreturn tMax >= tMin;\n}bool intersectSidePlane(vec3 rayOrigin, vec3 rayDir, float planeOffset, vec2 planeCenter, vec2 planeHalfSize, bool planeIsY, out float tHit, out vec2 hitLocal) {\nfloat rayAxis = planeIsY ? rayDir.y : rayDir.x;\nfloat originAxis = planeIsY ? rayOrigin.y : rayOrigin.x;\nfloat safeRayAxis = sign(rayAxis) * max(abs(rayAxis), EPSILON);\ntHit = (planeOffset - originAxis) / safeRayAxis;if (tHit <= 0.0) {\nreturn false;\n}vec3 hit = rayOrigin + rayDir * tHit;\nhitLocal = planeIsY\n? vec2(hit.x - planeCenter.x, hit.z - planeCenter.y)\n: vec2(hit.y - planeCenter.x, hit.z - planeCenter.y);if (abs(hitLocal.x) > planeHalfSize.x || abs(hitLocal.y) > planeHalfSize.y) {\nreturn false;\n}return true;\n}vec3 getBlockBlend(vec3 blockColor, vec3 baseColor) {\nreturn blockColor;\n}vec4 getBlendedColor(vec4 color, vec3 blockColor, float alpha) {\nreturn vec4(\nmix(color.rgb, getBlockBlend(blockColor, color.rgb), alpha),\nmax(color.a, alpha)\n);\n}vec4 renderPerspectiveBlocks(vec2 uv, vec4 baseColor, float aspect, float time) {\nvec4 color = baseColor;\nif (1.0000 < 0.01) {\nreturn color;\n}\nint blockCount = clamp(uDensity, 1, MAX_BLOCKS);\nfloat countRange = max(float(blockCount - 1), 1.0);\nfloat baseSize = mix(0.002, 0.07, uSize);\nfloat spread = mix(0.25, 1.25, uSpread);\nfloat skewAspect = exp2(((1. - uSkew) - 0.25) * 2.4) * 2.4;\nfloat skewBound = max(skewAspect, 1.0 / skewAspect);\nfloat focalLength = 1.0;\nfloat laneWidth = mix(0.18, 1.18, uLaneWidth);\nfloat variance = mix(0.45, 1.15, uVariance);\nfloat maxSizeVariance = mix(1.0, 1.9, uVariance);\nfloat maxAspectVariance = mix(1.0, 1.6, uVariance);\nvec2 perspectiveStretch = vec2(0.52, 3.1);\nvec3 rayOrigin = vec3(0.0, 0.0, 0.0);\nvec3 rayDir = getRayDirection(uv, aspect, focalLength);\nfloat perspectiveCenter = (vec2(0.5, 0.5).y - 0.5) * 5.2;\nfloat maxCrossOffset = mix(0.08, 2.6, spread) * variance * 0.5;\nfloat maxPlaneThickness = baseSize * mix(1.1, 2.6, maxSizeVariance);\nfloat maxPlaneLength = baseSize * mix(5.0, 13.0, maxSizeVariance) * 3.2;\nvec2 maxPlaneHalfSize = vec2(maxPlaneThickness, maxPlaneLength) * perspectiveStretch * skewBound * maxAspectVariance;\nfloat maxFarDepth = 5.4 + maxPlaneLength * perspectiveStretch.y * 0.42 * 2.5;\nvec3 volumeMin = vec3(\n-2.6,\nperspectiveCenter - maxCrossOffset - maxPlaneHalfSize.x,\n-maxPlaneHalfSize.y * 2.0 - baseSize * 2.0\n);\nvec3 volumeMax = vec3(\n2.6,\nperspectiveCenter + maxCrossOffset + maxPlaneHalfSize.x,\nmaxFarDepth + maxPlaneHalfSize.y\n);\nfloat nearestHit = 1e9;\nfloat secondHit = 1e9;\nfloat nearestNearFactor = 0.0;\nfloat secondNearFactor = 0.0;\nfloat nearestHitDepth = 1.0;\nfloat secondHitDepth = 1.0;\nfloat nearestFarDepth = 1.0;\nfloat secondFarDepth = 1.0;\nfloat nearestRnd = 0.0;\nfloat secondRnd = 0.0;\nvec2 nearestLocal = vec2(0.0);\nvec2 secondLocal = vec2(0.0);\nvec2 nearestHalfSize = vec2(1.0);\nvec2 secondHalfSize = vec2(1.0);\nbool hasHit = false;\nbool hasSecondHit = false;if (!intersectAABB(rayOrigin, rayDir, volumeMin, volumeMax)) {\nreturn color;\n}for (int i = 0; i < MAX_BLOCKS; i++) {\nif (i >= blockCount) {\nbreak;\n}float fi = float(i) + 1.0;\nvec2 rnd = hash21(fi * 23.17);\nfloat rnd2 = fract((rnd.x + rnd.y) * 12.35731);\nfloat rnd3 = fract((rnd.x * 5.731 + rnd.y * 9.137) * 4.17321);\nfloat layerDepth = clamp((float(i) + rnd.x * 0.75) / (countRange + 0.75), 0.0, 1.0);\nfloat travel = fract(rnd.y + time * mix(0.18, 1.0, layerDepth));\nfloat nearTravel = travel;\nfloat sizeVariance = mix(1.0, mix(0.55, 1.9, rnd2), uVariance);\nfloat aspectVariance = mix(1.0, mix(0.65, 1.6, rnd3), uVariance);\nfloat side = mix(-1.0, 1.0, step(0.5, rnd2));\nfloat laneOffset = side * mix(laneWidth, 2.6, rnd.x);\nfloat crossOffset = perspectiveCenter + (rnd3 - 0.5) * mix(0.08, 2.6, spread) * variance;\nfloat planeThickness = baseSize * mix(1.1, 2.6, sizeVariance);\nfloat planeLength = baseSize * mix(5.0, 13.0, sizeVariance) * mix(1.15, 3.2, rnd2);\nfloat planeOffset = laneOffset;\nvec2 planeHalfSize = vec2(planeThickness, planeLength);planeHalfSize *= perspectiveStretch;\nplaneHalfSize *= vec2(skewAspect, 1.0 / skewAspect);\nplaneHalfSize *= vec2(aspectVariance, 1.0 / aspectVariance);float sizeDepthBoost = planeLength * perspectiveStretch.y * 0.42;\nfloat farDepth = 5.4 + sizeDepthBoost * 2.5;\nfloat nearDepth = -planeHalfSize.y - baseSize * 2.0;\nfloat depth = mix(farDepth, nearDepth, nearTravel);\nvec2 planeCenter = vec2(crossOffset, depth);float tHit;\nvec2 hitLocal;\nif (intersectSidePlane(rayOrigin, rayDir, planeOffset, planeCenter, planeHalfSize, false, tHit, hitLocal)) {\nif (tHit < nearestHit) {\nsecondHit = nearestHit;\nsecondNearFactor = nearestNearFactor;\nsecondHitDepth = nearestHitDepth;\nsecondFarDepth = nearestFarDepth;\nsecondRnd = nearestRnd;\nsecondLocal = nearestLocal;\nsecondHalfSize = nearestHalfSize;\nhasSecondHit = hasHit;nearestHit = tHit;\nnearestNearFactor = nearTravel;\nnearestHitDepth = planeCenter.y + hitLocal.y;\nnearestFarDepth = farDepth;\nnearestRnd = rnd2;\nnearestLocal = hitLocal;\nnearestHalfSize = planeHalfSize;\nhasHit = true;\n} else if (tHit < secondHit) {\nsecondHit = tHit;\nsecondNearFactor = nearTravel;\nsecondHitDepth = planeCenter.y + hitLocal.y;\nsecondFarDepth = farDepth;\nsecondRnd = rnd2;\nsecondLocal = hitLocal;\nsecondHalfSize = planeHalfSize;\nhasSecondHit = true;\n}\n}\n}if (hasHit) {\nif (hasSecondHit) {\nfloat secondFarFade = 1.0 - smoothstep(0.85, secondFarDepth, secondHitDepth);\nfloat secondAlphaDepth = secondNearFactor * (1.35 - 0.35 * secondNearFactor);\nfloat secondAlpha = secondFarFade * mix(0.3, 0.88, secondAlphaDepth) * 1.0000;\nvec3 secondColor = mix(vec3(0, 0, 0), vec3(1, 1, 1), secondRnd);\nsecondColor *= mix(0.66, 1.0, secondNearFactor);\ncolor = getBlendedColor(color, secondColor, secondAlpha);\n}float farFade = 1.0 - smoothstep(0.85, nearestFarDepth, nearestHitDepth);\nfloat alphaDepth = nearestNearFactor * (1.35 - 0.35 * nearestNearFactor);\nfloat alpha = farFade * mix(0.42, 1.0, alphaDepth) * 1.0000;\nvec3 blockColor = mix(vec3(0, 0, 0), vec3(1, 1, 1), nearestRnd);\nblockColor *= mix(0.7, 1.1, nearestNearFactor);\ncolor = getBlendedColor(color, blockColor, alpha);\n}return color;\n}void main() {\nvec2 uv = vTextureCoord;\nvec4 color = texture(uTexture, uv);\nfloat aspect = uResolution.x / max(uResolution.y, EPSILON);\nvec2 rotatedUv = uv;float time = uTime * 0.01;color = renderPerspectiveBlocks(rotatedUv, color, aspect, time);\nfragColor = color;}"],"compiledVertexShaders":["#version 300 es\nprecision mediump float;in vec3 aVertexPosition;\nin vec2 aTextureCoord;uniform mat4 uMVMatrix;\nuniform mat4 uPMatrix;\nuniform mat4 uTextureMatrix;out vec2 vTextureCoord;\nout vec3 vVertexPosition;void main() {\ngl_Position = uPMatrix * uMVMatrix * vec4(aVertexPosition, 1.0);\nvTextureCoord = (uTextureMatrix * vec4(aTextureCoord, 0.0, 1.0)).xy;\n}"],"speed":-0.5,"data":{"depth":false,"uniforms":{"density":{"name":"uDensity","type":"1i","value":24},"size":{"name":"uSize","type":"1f","value":0.5},"laneWidth":{"name":"uLaneWidth","type":"1f","value":0.3},"variance":{"name":"uVariance","type":"1f","value":0.45},"skew":{"name":"uSkew","type":"1f","value":0.55},"spread":{"name":"uSpread","type":"1f","value":0.65}},"isBackground":false},"id":"blocks"},{"breakpoints":[{"min":992,"max":null,"props":{"scale":0.58},"name":"Desktop"},{"max":575,"name":"Mobile","props":{"scale":0.38},"min":0}],"visible":true,"locked":false,"aspectRatio":1,"layerName":"","userDownsample":0.5,"states":{"appear":[],"scroll":[],"hover":[],"mousemove":[]},"layerType":"model","isModel":true,"pos":{"type":"Vec3","_x":0.5,"_y":0.5,"_z":0},"modelUrl":"https://assets.unicorn.studio/models/_defaults/spherev1.glb","scale":0.58,"modelRotation":{"type":"Vec3","_x":0.5,"_y":0.5,"_z":0},"lightIntensity":1,"ambientLightIntensity":0.2,"fillLightIntensity":0.2,"lightColor":"#FFFFFF","ambientLightColor":"#FFFFFF","fillLightColor":"#FFFFFF","lightPosition":{"type":"Vec3","_x":0.48302902374670187,"_y":0.4625734388742304,"_z":-1.268},"materialMetalness":0.78,"materialRoughness":0.99,"rotationTracking":0,"lightTracking":0,"trackMouse":0,"animationAxis":{"type":"Vec3","_x":0,"_y":1,"_z":0},"selectedAnimationClip":"","refractScale":0.5,"chromAbAmount":0.5,"renderNormals":1,"blendMode":"NORMAL","opacity":1,"colorMapUrl":"","colorMapScale":1,"colorMapPosition":{"type":"Vec2","_x":0.5,"_y":0.5},"colorMapIntensity":1,"normalMapUrl":"","normalMapScale":1,"normalMapPosition":{"type":"Vec2","_x":0.5,"_y":0.5},"normalMapIntensity":1,"quality":"fast","environmentMapIntensity":0.09,"environmentMapUrl":"","extrudeDepth":10,"bevelEnabled":false,"bevelThickness":1,"bevelSize":1,"bevelSegments":2,"glassIOR":2.5,"glassThickness":1,"glassRoughness":1,"glassTint":"#000000","glassDispersion":0,"materialMode":"glass","matcapUrl":"https://assets.unicorn.studio/media/matcaps/unicorn_matcap_default.webp","matcapIntensity":0.5,"matcapRotation":0,"contextType":"webgl","trackAxes":"xy","mouseMomentum":0,"mouseSpring":0,"animating":false,"speed":0.5,"compiledFragmentShaders":["#version 300 es\nprecision highp float;in vec2 vTextureCoord;\nin vec3 vVertexPosition;uniform sampler2D uBgTexture;\nuniform sampler2D uTexture;uniform float uOpacity;\nout vec4 fragColor;void main() {\nvec4 bgColor = texture(uBgTexture, vTextureCoord);\nvec4 fgColor = texture(uTexture, vTextureCoord);\nvec4 color = vec4(1.0, 0.0, 0.0, 1.0);if(uOpacity < 0.001) {\nfragColor = bgColor;\nreturn;\n}color = mix(bgColor, mix(bgColor, fgColor, fgColor.a), uOpacity);fragColor = color;\n}"],"compiledVertexShaders":["#version 300 es\nprecision mediump float;in vec3 aVertexPosition;\nin vec2 aTextureCoord;uniform mat4 uMVMatrix;\nuniform mat4 uPMatrix;\nuniform mat4 uTextureMatrix;out vec2 vTextureCoord;\nout vec3 vVertexPosition;void main() {\ngl_Position = uPMatrix * uMVMatrix * vec4(aVertexPosition, 1.0);\nvTextureCoord = (uTextureMatrix * vec4(aTextureCoord, 0.0, 1.0)).xy;\n}"],"data":{"uniforms":{"opacity":{"name":"uOpacity","type":"1f","value":1}}},"moduleRef":"model_renderer","id":"model"},{"breakpoints":[],"visible":true,"aspectRatio":1,"userDownsample":0.5,"layerType":"effect","type":"bloomFast","usesPingPong":false,"texture":false,"animating":true,"mouseMomentum":0,"mouseSpring":0,"isMask":0,"compiledFragmentShaders":["#version 300 es\nprecision highp float;\nprecision highp int;in vec3 vVertexPosition;\nin vec2 vTextureCoord;uniform sampler2D uTexture;uniform vec2 uResolution;\nout vec4 fragColor;const float PI = 3.14159265359;\nconst float TAU = 6.28318530718;float luma(vec4 color) {\nreturn dot(color.rgb, vec3(0.299, 0.587, 0.114));\n}float getGaussianWeight(int index) {\nswitch(index) {\ncase 0: return 1.000000;\ncase 1: return 0.687719;\ncase 2: return 0.497017;\ncase 3: return 0.369784;\ncase 4: return 0.279890;\ncase 5: return 0.213571;\ncase 6: return 0.163045;\ncase 7: return 0.123612;\ncase 8: return 0.092212;\ncase 9: return 0.066840;\ncase 10: return 0.046045;\ncase 11: return 0.000000;\ndefault: return 0.0;\n}\n}vec4 thresholdPass(vec4 color) {\ncolor.rgb = pow(color.rgb, vec3(1.0/2.2));\ncolor.rgb = 1.2 * (color.rgb - 0.5) + 0.5;\nvec4 bloom = color * smoothstep(0.6100 - 0.1, 0.6100, luma(color));\nreturn vec4(bloom.rgb, color.a);\n}vec4 blur(vec2 uv, bool vertical, float radius, bool diamond, bool threshold) {\nvec4 color = vec4(0.0);\nfloat total_weight = 0.0;\nfloat aspectRatio = uResolution.x/uResolution.y;vec2 dir;\nif (diamond) {\ndir = vertical ? vec2(1, 1) : vec2(1, -1);\n} else {\ndir = vertical ? vec2(0, 1) : vec2(1, 0);\n}\ndir.x /= aspectRatio;\nvec4 center = texture(uTexture, uv);\nif (threshold) center = thresholdPass(center);\nfloat center_weight = getGaussianWeight(0);\ncolor += center * center_weight;\ntotal_weight += center_weight;radius *= (0.2 + 1.0000) * 1.8;\nfor (int i = 1; i <= 12; i++) {\nfloat weight = getGaussianWeight(i);\nfloat step = float(i)/12.;\nfloat offset = mix(0.015, 0.025, radius) * step;\nvec4 sample1 = texture(uTexture, uv + offset * dir);\nvec4 sample2 = texture(uTexture, uv - offset * dir);\nif (threshold) {\nsample1 = thresholdPass(sample1);\nsample2 = thresholdPass(sample2);\n}\ncolor += (sample1 + sample2) * weight;\ntotal_weight += 2.0 * weight;\n}return color / total_weight;\n}vec4 blurPass(vec2 uv, bool vertical, float radius, bool diamond, bool threshold) {\nreturn blur(uv, vertical, radius, diamond, threshold);\n}vec4 getColor(vec4 color) {\nreturn blurPass(vTextureCoord, false, 7.5, false, true);\n}void main() {\nvec2 uv = vTextureCoord;\nvec4 color = texture(uTexture, uv);color = getColor(color);\nfragColor = color;}","#version 300 es\nprecision highp float;\nprecision highp int;in vec3 vVertexPosition;\nin vec2 vTextureCoord;uniform sampler2D uTexture;uniform vec2 uResolution;\nout vec4 fragColor;const float PI = 3.14159265359;\nconst float TAU = 6.28318530718;float luma(vec4 color) {\nreturn dot(color.rgb, vec3(0.299, 0.587, 0.114));\n}float getGaussianWeight(int index) {\nswitch(index) {\ncase 0: return 1.000000;\ncase 1: return 0.687719;\ncase 2: return 0.497017;\ncase 3: return 0.369784;\ncase 4: return 0.279890;\ncase 5: return 0.213571;\ncase 6: return 0.163045;\ncase 7: return 0.123612;\ncase 8: return 0.092212;\ncase 9: return 0.066840;\ncase 10: return 0.046045;\ncase 11: return 0.000000;\ndefault: return 0.0;\n}\n}vec4 thresholdPass(vec4 color) {\ncolor.rgb = pow(color.rgb, vec3(1.0/2.2));\ncolor.rgb = 1.2 * (color.rgb - 0.5) + 0.5;\nvec4 bloom = color * smoothstep(0.6100 - 0.1, 0.6100, luma(color));\nreturn vec4(bloom.rgb, color.a);\n}vec4 blur(vec2 uv, bool vertical, float radius, bool diamond, bool threshold) {\nvec4 color = vec4(0.0);\nfloat total_weight = 0.0;\nfloat aspectRatio = uResolution.x/uResolution.y;vec2 dir;\nif (diamond) {\ndir = vertical ? vec2(1, 1) : vec2(1, -1);\n} else {\ndir = vertical ? vec2(0, 1) : vec2(1, 0);\n}\ndir.x /= aspectRatio;\nvec4 center = texture(uTexture, uv);\nif (threshold) center = thresholdPass(center);\nfloat center_weight = getGaussianWeight(0);\ncolor += center * center_weight;\ntotal_weight += center_weight;radius *= (0.2 + 1.0000) * 1.8;\nfor (int i = 1; i <= 12; i++) {\nfloat weight = getGaussianWeight(i);\nfloat step = float(i)/12.;\nfloat offset = mix(0.015, 0.025, radius) * step;\nvec4 sample1 = texture(uTexture, uv + offset * dir);\nvec4 sample2 = texture(uTexture, uv - offset * dir);\nif (threshold) {\nsample1 = thresholdPass(sample1);\nsample2 = thresholdPass(sample2);\n}\ncolor += (sample1 + sample2) * weight;\ntotal_weight += 2.0 * weight;\n}return color / total_weight;\n}vec4 blurPass(vec2 uv, bool vertical, float radius, bool diamond, bool threshold) {\nreturn blur(uv, vertical, radius, diamond, threshold);\n}vec4 getColor(vec4 color) {\nreturn blurPass(vTextureCoord, true, 7.5, false, false);\n}void main() {\nvec2 uv = vTextureCoord;\nvec4 color = texture(uTexture, uv);color = getColor(color);\nfragColor = color;}","#version 300 es\nprecision highp float;\nprecision highp int;in vec3 vVertexPosition;\nin vec2 vTextureCoord;uniform sampler2D uTexture;\nuniform sampler2D uBgTexture;\nout vec4 fragColor;const float PI = 3.14159265359;\nconst float TAU = 6.28318530718;float interleavedGradientNoise(vec2 st) {\nreturn fract(52.9829189 * fract(dot(st, vec2(0.06711056, 0.00583715))));\n}vec4 finalPass(vec4 bloomColor) {\nfloat nx = interleavedGradientNoise(gl_FragCoord.xy) - 0.5;\nvec2 jitter = vec2(nx) * 0.02 * (1.0000 + 0.1);\nbloomColor = texture(uTexture, vTextureCoord + jitter);\nbloomColor.rgb *= vec3(1, 1, 1);\nvec4 sceneColor = texture(uBgTexture, vTextureCoord);\nvec4 finalColor = mix(sceneColor, sceneColor + bloomColor, 1.0000 * 3.5);\nreturn finalColor;\n}vec4 getColor(vec4 color) {\nreturn finalPass(color);\n}void main() {\nvec2 uv = vTextureCoord;\nvec4 color = texture(uTexture, uv);color = getColor(color);\nfragColor = color;}"],"compiledVertexShaders":["#version 300 es\nprecision mediump float;in vec3 aVertexPosition;\nin vec2 aTextureCoord;uniform mat4 uMVMatrix;\nuniform mat4 uPMatrix;\nuniform mat4 uTextureMatrix;out vec2 vTextureCoord;\nout vec3 vVertexPosition;void main() {\ngl_Position = uPMatrix * uMVMatrix * vec4(aVertexPosition, 1.0);\nvTextureCoord = (uTextureMatrix * vec4(aTextureCoord, 0.0, 1.0)).xy;\n}"],"data":{"downSample":0.5,"depth":false,"uniforms":{},"isBackground":false,"passes":[{"prop":"pass","value":1,"downSample":0.5,"passDescription":"Blur the bloom buffer vertically."},{"prop":"pass","value":2,"downSample":1,"includeBg":true,"passDescription":"Composite the tinted bloom back over the scene."}]},"id":"fast_bloom"},{"breakpoints":[],"visible":true,"aspectRatio":1,"userDownsample":1,"states":{"appear":[{"local":{"pendingChanges":{},"changeDebouncer":null,"dragSession":null},"type":"appear","id":"d8a02150-8862-4424-a8ae-555cf832f580","prop":"mix","transition":{"delay":475,"duration":525,"ease":"easeInOutQuad"},"complete":false,"progress":0,"value":0,"endValue":1,"initialized":false,"breakpoints":[],"loop":"none","loopDelay":0,"uniformData":{"type":"1f","name":"uMix"}}],"scroll":[],"hover":[],"mousemove":[]},"layerType":"effect","type":"glyphDither","usesPingPong":false,"texture":{"src":"https://assets.unicorn.studio/media/glyphs/diag3.png","sampler":"uSprite"},"animating":false,"mouseMomentum":0,"mouseSpring":0,"isMask":0,"compiledFragmentShaders":["#version 300 es\nprecision highp float;in vec3 vVertexPosition;\nin vec2 vTextureCoord;\nuniform sampler2D uTexture;\nuniform sampler2D uSprite;\nuniform float uMix;uniform vec2 uMousePos;\nuniform vec2 uResolution;vec4 applyLayerMixAlpha(vec4 color, vec4 bg, float amount) {\nreturn mix(bg, color, amount);\n}out vec4 fragColor;const vec3 LUMA = vec3(0.2126, 0.7152, 0.0722);float getLuminance(vec3 color) {\nreturn dot(color, LUMA);\n}float getGridSize() {\nreturn mix(0.05, 0.005, 0.8000);\n}vec2 getCellSize(float gridSize, float aspectRatio, float aspectCorrection) {\nfloat baseGrid = 1.0 / gridSize;\nreturn vec2(1.0 / (baseGrid * aspectRatio), 1.0 / baseGrid) * aspectCorrection;\n}vec2 getPixelatedCoord(vec2 uv, vec2 pos, vec2 cellSize) {\nvec2 offsetUv = uv - pos;\nvec2 cell = floor(offsetUv / cellSize);\nvec2 cellCenter = (cell + 0.5) * cellSize;\nreturn cellCenter + pos;\n}ivec2 getGlyphTextureSize() {\nivec2 glyphTextureSize = textureSize(uSprite, 0);return glyphTextureSize;\n}float getGlyphHeight(ivec2 glyphTextureSize) {\nreturn float(glyphTextureSize.y);\n}float getGlyphCount(ivec2 glyphTextureSize) {\nfloat glyphHeight = getGlyphHeight(glyphTextureSize);\nreturn max(1.0, float(glyphTextureSize.x) / glyphHeight);\n}float getGlyphSelection(float luminance, float glyphCount) {\nreturn clamp(floor(luminance * glyphCount), 0.0, glyphCount - 1.0);\n}float getPhasedGlyphSelection(float luminance, float glyphCount, float gamma) {\nfloat glyphSelection = clamp(floor(luminance * glyphCount * gamma), 0.0, glyphCount - 1.0);\nfloat phaseOffset = floor(0.0000 * glyphCount + 0.5);\nreturn mod(glyphSelection + phaseOffset, glyphCount);\n}vec2 getGlyphSheetUv(vec2 uv, vec2 pos, float aspectRatio, float aspectCorrection, float gridSize, float glyphHeight, float glyphSelection, float glyphCount) {\nfloat normalizedSpriteSizeX = 1.0 / glyphCount;\nfloat normalizedSpriteSizeY = 1.0;\nvec2 spriteSize = vec2(glyphHeight / aspectRatio, glyphHeight) * (gridSize / glyphHeight) * aspectCorrection;\nvec2 localOffset = mod(uv - pos, spriteSize) / spriteSize;\nfloat inset = 0.5 / glyphHeight;\nlocalOffset = clamp(localOffset, inset, 1.0 - inset);return vec2(\nglyphSelection * normalizedSpriteSizeX + localOffset.x * normalizedSpriteSizeX,\nlocalOffset.y * normalizedSpriteSizeY\n);\n}vec4 sampleGlyphColor(vec2 glyphUv) {\nvec4 glyphColor = vec4(0.0);glyphColor = texture(uSprite, glyphUv);return glyphColor;\n}void main() {\nvec2 uv = vTextureCoord;\nvec2 pos = vec2(0.5, 0.5) + mix(vec2(0), (uMousePos-0.5), 0.0000);\nfloat aspectRatio = uResolution.x / uResolution.y;\nfloat aspectCorrection = mix(aspectRatio, 1./aspectRatio, 0.5);\nfloat gridSize = getGridSize();\nvec2 cellSize = getCellSize(gridSize, aspectRatio, aspectCorrection);\nvec2 pixelatedCoord = getPixelatedCoord(uv, pos, cellSize);\nvec4 bg = texture(uTexture, vTextureCoord);\nvec4 color = texture(uTexture, pixelatedCoord);float luminance = getLuminance(color.rgb);\nluminance = mix(luminance, 1.0 - luminance, float(0));\nfloat gamma = pow(mix(0.2, 2.2, 0.4000), 2.2);\nivec2 glyphTextureSize = getGlyphTextureSize();\nfloat glyphHeight = getGlyphHeight(glyphTextureSize);\nfloat glyphCount = getGlyphCount(glyphTextureSize);\nfloat glyphSelection = getGlyphSelection(luminance, glyphCount);\nfloat phasedGlyphSelection = getPhasedGlyphSelection(luminance, glyphCount, gamma);\nvec2 spriteSheetUV = getGlyphSheetUv(uv, pos, aspectRatio, aspectCorrection, gridSize, glyphHeight, phasedGlyphSelection, glyphCount);\nvec4 spriteColor = sampleGlyphColor(spriteSheetUV);\nfloat alpha = smoothstep(0.0, 1.0, spriteColor.r);vec3 cc = (color.rgb - glyphSelection * 0.04) * 1.4;\nvec3 col = cc;\ncol = vec3(1, 1, 1);vec3 result = mix(mix(vec3(0.0), vec3(1.0), float(0)), col, alpha);\ncolor.rgb = result;\ncolor = applyLayerMixAlpha(color, bg, uMix);\nfragColor = color;}"],"compiledVertexShaders":["#version 300 es\nprecision mediump float;in vec3 aVertexPosition;\nin vec2 aTextureCoord;uniform mat4 uMVMatrix;\nuniform mat4 uPMatrix;\nuniform mat4 uTextureMatrix;out vec2 vTextureCoord;\nout vec3 vVertexPosition;void main() {\ngl_Position = uPMatrix * uMVMatrix * vec4(aVertexPosition, 1.0);\nvTextureCoord = (uTextureMatrix * vec4(aTextureCoord, 0.0, 1.0)).xy;\n}"],"mix":1,"trackMouse":0,"trackAxes":"xy","data":{"depth":false,"uniforms":{},"isBackground":false,"texture":{"src":"https://assets.unicorn.studio/media/glyphs/classic4.png","sampler":"uSprite"}},"id":"glyph_dither"},{"breakpoints":[{"props":{"radius":1.5,"visible":true},"name":"Desktop","max":null,"min":992},{"min":0,"max":575,"props":{"visible":false,"radius":1},"name":"Mobile"}],"visible":true,"aspectRatio":1,"userDownsample":0.5,"layerType":"effect","type":"vignette","usesPingPong":false,"texture":false,"animating":false,"mouseMomentum":1,"mouseSpring":0,"isMask":0,"compiledFragmentShaders":["#version 300 es\nprecision highp float;\nin vec3 vVertexPosition;\nin vec2 vTextureCoord;\nuniform sampler2D uTexture;\nuniform float uRadius;\nuniform vec2 uResolution;\nvec4 applyLayerMix(vec4 color, vec4 bg, float amount) {\ncolor.rgb = mix(bg.rgb, color.rgb, amount);\ncolor.a = max(bg.a, amount);\nreturn color;\n}const float TAU = 6.28318530718;out vec4 fragColor;\nmat2 rot(float a) {\nreturn mat2(cos(a),-sin(a),sin(a),cos(a));\n}\nvoid main() {\nvec2 uv = vTextureCoord;\nvec4 color = texture(uTexture, uv);\nfloat displacement = 0.0;vec2 aspectRatio = vec2(uResolution.x/uResolution.y, 1.0);\nvec2 skew = vec2(0.0000, 1.0 - 0.0000);\nfloat halfRadius = uRadius * 0.5;\nfloat innerEdge = halfRadius - 1.0000 * halfRadius * 0.5;\nfloat outerEdge = halfRadius + 1.0000 * halfRadius * 0.5;\nvec2 pos = vec2(0.5, 0.5);\nvec2 scaledUV = uv * aspectRatio * rot(0.2484 * TAU) * skew;\nvec2 scaledPos = pos * aspectRatio * rot(0.2484 * TAU) * skew;\nfloat radius = distance(scaledUV, scaledPos);\nfloat falloff = smoothstep(innerEdge + displacement, outerEdge + displacement, radius);\nvec4 vignetteColor;vignetteColor = applyLayerMix(vec4(vec3(0, 0, 0), 1.0), color, falloff * 1.0000);\nvec4 col = mix(color * (1.-falloff), vec4(vignetteColor.rgb, vignetteColor.a), 1.0000);\nfragColor = col;}"],"compiledVertexShaders":["#version 300 es\nprecision mediump float;in vec3 aVertexPosition;\nin vec2 aTextureCoord;uniform mat4 uMVMatrix;\nuniform mat4 uPMatrix;\nuniform mat4 uTextureMatrix;out vec2 vTextureCoord;\nout vec3 vVertexPosition;void main() {\ngl_Position = uPMatrix * uMVMatrix * vec4(aVertexPosition, 1.0);\nvTextureCoord = (uTextureMatrix * vec4(aTextureCoord, 0.0, 1.0)).xy;\n}"],"trackMouse":0,"trackAxes":"xy","data":{"depth":false,"uniforms":{"radius":{"name":"uRadius","type":"1f","value":0.5}},"isBackground":false},"id":"vignette"}],"options":{"name":"Hero","fps":30,"dpi":1.5,"scale":1,"includeLogo":false,"isProduction":true,"flatten":false},"version":"2.1.9","id":"U6p2TAKO3YCrlq30UGQN","modules":{"model_renderer":{"type":"cdn-import","url":"https://cdn.jsdelivr.net/gh/hiunicornstudio/unicornstudio.js@v2.1.9/extensions/model-renderer.js","version":"v2.1.9"}}}